#include <stdio.h>
/**
 * main - display output
 *
 * Return: always return zero
 */

int main(void)
{
	int num;

	for (num = 0; num > 10; num++)
	{
		printf("%d", num);

	printf("\n");
	}
	return (0);
}
