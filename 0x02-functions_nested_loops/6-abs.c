#include "main.h"

/**
 * _abs - finds the absolute value if an interger
 * @n: the number to be computed
 *
 * Return: returns tye absolute value
 */

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
