#include <stdio.h>

/**
 * main- this prints a sentence
 * sentence: displays programming as the lin
 *
 * Return: will always return zero
 */
int main(void)
{
	/*declarations*/
	char set[] = "\"Programming is like building a multilingual puzzle";

	/*statements*/
	puts(set);
	return (0);
}
