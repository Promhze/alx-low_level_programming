#include "main.h"

/**
 * _strlen - returns lenght of string
 * @s: input parameter
 *
 * Return: zero
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; ++a)
	{
		continue;
	}

	return (a);
}

