#include "main.h"

/**
 * main - tests function that prints if integer is positive or negative
 * Return: always return 0;
 */

int main(void)
{
	int i;

	i = 0; /*assigns value to the 'i' interger*/
	positive_or_negative(i);

	return (0);
}

